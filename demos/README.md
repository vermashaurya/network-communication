# Browser Demos

Browser-safe demos for algorithms from socket projects.

- Checksum and parity interactions are integrated into `site/`.
- These demos intentionally avoid raw socket APIs so they can run on GitHub Pages.
